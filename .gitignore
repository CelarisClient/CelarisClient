# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.tauri/

# Build output / secrets
src-tauri/target/
/target/
.env
.env.*
*.env

# --- Special Admin build (NEVER commit/push) ---
# Real Admin tab UI + the Rust admin bridge. Player builds use the stub instead.
src/admin/AdminPanel.real.tsx
src-tauri/src/admin.rs
# Local admin token (also stored at runtime in app_config_dir/admin.json)
admin.json

# Local agent/instructions notes (kept local)
AGENTS.md
CLAUDE.md
linux-repo/
