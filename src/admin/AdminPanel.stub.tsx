// Placeholder for normal/player builds. The real Admin tab is git-ignored and
// only wired in when building with VITE_ADMIN=1 (see vite.config.ts alias).
export const ADMIN_ENABLED = false;

export default function AdminPanel() {
  return null;
}
